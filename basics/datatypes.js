"use strict"; // newer version of js

//alert(test); // it'll work in browser

//number, bigint, string, boolean, null, undefined, symbol => unique, object

let age = 1
let isAge = false
let state = null
console.log(typeof state);